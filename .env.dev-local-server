# Use separate dev directory for happy data
# Useful to allow having separate auth for local dev / production dogfooding
GENIE_HOME_DIR=~/.genie-dev

# Local development server
GENIE_CONTENT_SERVER_URL=https://locahost:8000
GENIE_RELAY_SERVER_URL=http://localhost:3005
GENIE_WEB_URL=http://localhost:8081
GENIE_OIDC_CLIENT_ID=<genie-oidc-client-id>

DEBUG=1
# DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=true

# We have this in bin too for production
NODE_NO_WARNINGS=1

